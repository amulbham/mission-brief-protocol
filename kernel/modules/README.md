# Kernel Modules

Maintainable source sections for the boot prompt live here. Module order will be declared by the build manifest introduced with Kernel 1.0.

Each normative module must identify its authority, dependencies, and corresponding tests.
