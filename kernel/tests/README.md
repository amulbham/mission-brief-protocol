# Kernel Tests

Tests are divided into focused conformance checks and inherited regression checks.

A version may seal only when its focused suite and every inherited regression suite pass. Test evidence must name the executable files, counts, and registry digest.
