# Kernel Releases

Each released Kernel Track version receives an immutable directory containing the compiled boot prompt, registry manifest, validation summary, and SHA-256 digest.

Existing releases are never edited. Corrections require a new version.
