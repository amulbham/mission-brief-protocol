# Kernel Registry

This directory contains machine-readable records for flashed invariants, components, versions, dependencies, and supersessions.

Registry records carry authority only after their associated validation gates pass. Historical claims imported from source documents remain provenance until explicitly flashed in the Kernel Track.
